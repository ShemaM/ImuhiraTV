# Articles Directory

This directory contains the dynamic route for individual article pages.

## Contents

- **`[slug].tsx`** - Next.js dynamic route page that renders individual articles based on their slug

## Route Structure

This creates routes like:
- `/en/articles/article-title`
- `/fr/articles/titre-article`

The `[slug]` parameter is used to fetch and display the corresponding article from the mock data.
