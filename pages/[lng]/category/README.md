# Category Directory

This directory contains the dynamic route for category pages.

## Contents

- **`[slug].tsx`** - Next.js dynamic route page that displays articles filtered by category

## Route Structure

This creates routes like:
- `/en/category/politics`
- `/fr/category/culture`

The `[slug]` parameter is used to filter and display articles belonging to that category.
